import React from "react";

const Login = () => {
    return ([
        <section className="hero">
            <h1>welcome to Login pages</h1>
        </section>
    ]);
}

export default Login;